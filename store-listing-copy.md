# Chrome 应用商店详情页文案（htmlto.link 浏览器插件）

> 用途：直接复制到 Chrome Web Store 详情页。已按「去掉右键菜单 + 新增可关闭自动注入 + 引导页」的最新功能撰写。
> 匿名链接约 24 小时、登录可长期管理这一关键预期，务必在文案里写明，降低卸载与差评。

---

## 1. 名称（Title）

```
htmlto.link - 一键把 HTML/AI 代码变成在线链接
```

## 2. 简短描述（Short description）

```
把 ChatGPT/Claude 生成的 HTML 一键变成可分享链接，自动复制，支持 Markdown 模板。
```

## 3. 详细描述（Detailed description）

```
把 AI 生成的网页，1 秒变成可分享的在线链接。

有没有遇到过：让 AI 写了个漂亮网页，想发给同事或客户，却只能截图、发代码文件，对方根本打不开？htmlto.link 解决的就是这个痛点。

✅ 一键生成链接
在 ChatGPT、Claude、DeepSeek、v0、Kimi、通义千问、豆包、Gemini 等 AI 页面里，代码块右上角会自动出现「🚀 生成链接」按钮，点一下：上传 → 生成 https://htmlto.link/s/xxxxx → 自动复制到剪贴板，直接发微信 / 钉钉 / 飞书。

✅ 弹窗粘贴即发
点击浏览器右上角图标，粘贴 HTML 或 Markdown，一键发布。支持自定义文件名、发布历史记录与一键复制。

✅ Markdown 也能发，内置 9 套渲染模板
简洁、备忘录、字节范、暗黑科技、线圈笔记本、中国传统、波普艺术、温暖柔和、阿里橙，同一篇文档一键换风格。

✅ 开发者友好
可自定义 API 节点（生产 / 本地），绑定个人 API Token 长期管理自己发布的内容。

🔒 隐私与透明
- 代码只会发送到 https://htmlto.link 用于生成链接，绝不用于其他用途。
- 插件只在你主动点击发布时才上传，不会后台扫描或收集你的聊天内容。
- 可以在设置里随时关闭「自动注入按钮」，不影响在弹窗里手动发布。

⏳ 关于链接有效期
未登录发布的匿名链接约 24 小时后失效；登录 htmlto.link 并绑定 Token 后，可长期保存、管理和续期。

如果你在使用中遇到任何问题，欢迎通过 https://htmlto.link 联系我们。
```

---

## 4. 权限说明（Permission justification）

| 权限 | 为什么需要 |
|------|-----------|
| 读取和更改你在 11 个 AI 网站上的数据 | 仅在 ChatGPT / Claude / DeepSeek / v0 / Kimi 等页面的**代码块右上角注入一个发布按钮**，方便你一键发布；不会读取你的聊天内容，也不会修改页面其他内容。可在设置里一键关闭。 |
| 存储（storage） | 记住你的发布历史、自定义 API 节点与 Token、以及「是否自动注入按钮」的偏好设置。 |
| 剪贴板写入（clipboardWrite） | 发布成功后自动把生成的链接复制到剪贴板，方便你直接粘贴分享。 |
| 与 htmlto.link 通信（host_permissions） | 仅用于把你要发布的代码上传到 https://htmlto.link 生成链接。 |

---

## 5. 隐私实践（Privacy practices）

- **收集的数据**：仅在用户主动点击发布时，上传其粘贴/选中的 HTML 或 Markdown 代码；设置里填写 API Token 时保存在浏览器本地。
- **数据用途**：仅用于生成可访问的分享链接。
- **不收集**：不收集浏览历史、聊天记录、个人身份信息；不上传与发布无关的页面内容。
- **数据存储**：匿名链接约 24 小时后自动清理；登录用户的页面按其账号规则保留。
- **关闭方式**：在扩展设置页（右键扩展图标 → 选项）可关闭自动注入；卸载时不会残留远程数据。

---

## 6. 商店截图建议（Screenshot ideas）

1. 封面：AI 聊天页面代码块右上角的「🚀 生成链接」按钮 + 生成出的链接。
2. 弹窗：粘贴 HTML / Markdown → 发布 → 结果卡片（含 24h 有效期提示）。
3. 设置页：自动注入开关 + API 节点 + Token 绑定。
4. Markdown 多模板效果对比。

> 说明：`generate-promo-images.js` / `generate-promo-images-zh.js` 会生成这些素材，跑一次即可更新图片。
